#%Module1.0#####################################################################
##
## multiqc modulefile
## By Alan Orth <a.orth@cgiar.org>
## September, 2020
##

set appname     multiqc
set version     1.6
set prefix      /export/apps/${appname}/${version}
set exec_prefix ${prefix}/bin
set msg         "MultiQC is a reporting tool that parses summary statistics from results and log files generated by other bioinformatics tools.\nOfficial Site: http://multiqc.info/docs/"

# multiqc/1.6 uses a virtual environment created with CentOS's python3 so we
# should make sure no python environment modules get loaded that might mess
# with the paths.
conflict $appname python

module-whatis $msg

prepend-path    PATH       ${exec_prefix}
prepend-path    PYTHONPATH ${prefix}/lib/python3.6/site-packages

# vim: set sw=4 ts=4:
